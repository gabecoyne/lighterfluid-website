<section class="section" style="padding-top:128px;padding-bottom:128px;text-align:center;">
  <div class="container narrow">
    <span class="eyebrow"><span class="dot"></span> 404 · Page not found</span>
    <h1 class="lf-h1" style="font-family:var(--lf-serif);font-weight:350;font-size:clamp(40px,5vw,64px);line-height:1.05;letter-spacing:-0.02em;margin-top:24px;text-wrap:balance;">
      Couldn't find that page.<br/><em style="font-style:italic;color:var(--lf-ember);">Let's get you back to one that exists.</em>
    </h1>
    <p style="font-family:var(--lf-serif);font-style:italic;font-size:19px;color:var(--lf-graphite);max-width:560px;margin:32px auto 0;line-height:1.5;">
      The page you were looking for has moved, or the link you followed is wrong. Pick a destination below.
    </p>
    <div style="display:flex;gap:14px;justify-content:center;margin-top:48px;flex-wrap:wrap;">
      <a class="btn" href="{{ routes.root_url }}">Home</a>
      <a class="btn ghost" href="{{ routes.root_url }}pages/implementation">AI Install</a>
      <a class="btn ghost" href="{{ routes.root_url }}pages/coaching">Founder Coaching</a>
      <a class="btn ember" href="{{ routes.root_url }}pages/contact">Book a call</a>
    </div>
  </div>
</section>
